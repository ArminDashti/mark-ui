# Optional overlay: host port publish (local / explicit publish_port).
# On t3, leave publish_port empty — HAProxy reaches mark-ui:80 on t3-net.

services:
  mark-ui:
    ports:
      - target: 80
        published: ${PUBLISH_PORT:-5180}
        protocol: tcp
